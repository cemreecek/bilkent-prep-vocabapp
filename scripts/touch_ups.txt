Set 1: Flashcard definitions are missing (only words were in tables). Practices require manual structural parsing.
Set 2: Flashcard definitions are missing (only words were in tables). Practices require manual structural parsing.
Set 3: Flashcard definitions are missing (only words were in tables). Practices require manual structural parsing.
Set 4: Flashcard definitions are missing (only words were in tables). Practices require manual structural parsing.
Set 5: Flashcard definitions are missing (only words were in tables). Practices require manual structural parsing.
Set 6: Flashcard definitions are missing (only words were in tables). Practices require manual structural parsing.
Set 7: Flashcard definitions are missing (only words were in tables). Practices require manual structural parsing.
Set 8: Flashcard definitions are missing (only words were in tables). Practices require manual structural parsing.
